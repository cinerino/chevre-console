<% if (messages.message) { %>
<script>
    $.notify({
        // options
        message: '<%= messages.message %>'
    }, {
        // settings
        type: 'success',
        placement: {
            from: 'top',
            align: 'right'
        }
    });
</script>
<% } %>