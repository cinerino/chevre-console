<% if (message !== '') { %>
<script>
    $.notify({
        // options
        message: '<%= message %>'
    }, {
        // settings
        type: 'danger',
        placement: {
            from: 'top',
            align: 'right'
        },
    });
</script>
<% } %>